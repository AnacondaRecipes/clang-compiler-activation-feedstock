build_parameters:
  - "--suppress-variables"

# Stage 1: Build clang-compiler-activation to create clang_bootstrap 21
staging_channel_upload_target: llvm-21.1.8-stage1-build-0

channels:
  # Stage 0 channel has clangdev 21 AND cctools 1024.3 (built with LLVM 21)
  - https://staging.continuum.io/pbp/llvm-21.1.8-stage0-build-0
