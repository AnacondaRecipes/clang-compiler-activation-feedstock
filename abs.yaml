# the conda build parameters to use
build_parameters:
  - "--suppress-variables"

staging_channel_upload_target: llvm-17.0.6-stage2-attempt-1

channels:
  - https://staging.continuum.io/prefect/llvm-17.0.6-stage2