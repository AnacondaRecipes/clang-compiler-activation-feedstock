# the conda build parameters to use
build_parameters:
  - "--suppress-variables"
  # - "--error-overlinking"
  # TODO: FIX OVERLINKING ERRORS

upload_without_merge: True
upload_to_staging_channel_in_pr: True
channels: [ llvmdev17 ]