# the conda build parameters to use
build_parameters:
  - "--suppress-variables"
  - "--skip-existing"

channels:
  - https://staging.continuum.io/prefect/llvm-19.1.1-stage0-build-0

staging_channel_upload_target: llvm-19.1.1-stage0-build-0